# Services.wsdl should be accessed without entering credentials.
# Services.wsdl should contain service definition, i.e. <wsdl:service> tag.
# valid values: relative or global paths or URL
wsdlPath=file:Services.wsdl

# <domain>\\<user>
username=mas\\user

password=Qwer!234

# valid values: from -128 to 127
priority=0

# coma separated values of emails corresponding to lists to be archived
# (!) list won't be archived if its email address is not here
lists=labuser@mas.test, user@mas.test,example-of-list@domain.com

# whether connector should remove archived messages
# valid values: true, false
mailboxCleanup=false